import React from 'react'
import { Button } from 'react-bootstrap';

const CustomButton = ({type,className, size,variant}) => {
    return (
        <Button></Button>
    );
}

export default CustomButton;